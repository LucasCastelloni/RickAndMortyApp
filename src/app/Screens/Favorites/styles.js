import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
  innerContainer: {
    paddingTop: 10,
  },
  scrollContainer: {
    width: '100%',
  },
});

export default styles;
