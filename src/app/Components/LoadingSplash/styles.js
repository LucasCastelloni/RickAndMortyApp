import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    alignItems: 'center',
    alignSelf: 'center',
    flex: 1,
    alignContent: 'center',
    justifyContent: 'center',
    width: '100%',
  },
  animation: {
    width: '70%',
  },
});
